# Production CI — runs on every push to `prod`
# Render/Koyeb auto-deploy when connected to the prod branch in their dashboard.
# Optional: add RENDER_DEPLOY_HOOK GitHub secret to trigger Render redeploy from CI.

name: Production

on:
  push:
    branches: [prod]
  pull_request:
    branches: [prod]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install frontend deps
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Install backend deps
        run: npm ci --prefix backend

      - name: Build backend
        run: npm run build --prefix backend

      - name: Build production Docker image
        run: docker build -f Dockerfile.production -t tiny-url-prod:ci .

  trigger-render:
    needs: build-and-test
    if: github.ref == 'refs/heads/prod' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          if [ -n "$RENDER_DEPLOY_HOOK" ]; then
            curl -fsS -X POST "$RENDER_DEPLOY_HOOK"
            echo "Render deploy triggered"
          else
            echo "RENDER_DEPLOY_HOOK not set — Render will deploy via GitHub integration"
          fi
